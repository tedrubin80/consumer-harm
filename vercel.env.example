# Vercel → Project → Settings → Environment Variables
# Apply to Production, Preview, Development

# Public URL of the Streamlit app on Railway (update after first Railway deploy)
NEXT_PUBLIC_APP_URL=https://consumer-harm-production.up.railway.app

# Shown on the static landing page (optional; edit web/index.html or use Railway URL in /app rewrite)
GITHUB_REPO=https://github.com/tedrubin80/consumer-harm
